stripes are 1:1 black to white-space. 

X-scale set to 1, so is y-scale.

for vertical, stripes x-axis offset is -0.250 to make stripes appear evenly in the centre. 

for horizontal, x-axis is set to 0 seems to be suitable.

default is vertical. I create horizontal by setting orientation to 90.

